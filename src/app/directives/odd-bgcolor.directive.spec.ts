import { OddBGcolorDirective } from './odd-bgcolor.directive';

describe('OddBGcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new OddBGcolorDirective();
    expect(directive).toBeTruthy();
  });
});
